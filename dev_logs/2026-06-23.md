# 开发日志 · 2026-06-23

## 今日完成

### 全文搜索 — 设计落档

- [x] 新增 `docs/search-design.md`（11 节完整设计）：场景、分期、LIKE vs FTS5 选型、SQL/`escape_like`、API、前端交互、与播放器整合、边界、P6/P7 任务验收、FTS5 升级路径
- [x] 同步更新文档：`requirements.md`（2.7 已立项）、`tech-spec.md`（+2 API）、`execution-plan.md`（+P6/P7）、`design-spec.md`（+搜索栏组件）、`CLAUDE.md`（文档索引）、`README.md`（路线图标注）
- [x] 文档提交：`def35e9 docs: 全文搜索设计落档 + 同步各规范文档`

### P6 — 单文件内全文搜索 ✅ 已实现

- [x] `utils.py`：新增 `escape_like()`，转义 SQL LIKE 通配符（`% _ \`）
- [x] `app.py`：新增 `GET /api/file/<id>/search`，`func.lower(text).like(kw, escape='\\')` 大小写不敏感子串匹配
- [x] `templates/detail.html`：转写区标题改为 `transcript-header`（标题 + 搜索栏，仅 completed 渲染）；新增搜索交互 JS
- [x] `static/style.css`：搜索栏、`.search-hit`（淡黄）/`.search-current`（深黄 + 蓝 outline）/`mark` 样式

### 关键实现要点

1. **搜索逻辑收拢在 `doSearch()`**：防抖 300ms → fetch API → 命中段加 `.search-hit`、`↑/↓`+回车循环导航、`Esc`/`✕` 清除、计数 `当前/总数`
2. **高亮由前端完成**（`highlight()` + `esc()` 转义）：规避 Python(Unicode 码点) 与 JS(UTF-16 码元) 在 emoji 文本上的索引错位
3. **与播放器共存**：新增 `searchActive` 标志，搜索激活期间 `timeupdate` 的 `scrollIntoView` 被守卫跳过（仍保留 `.active` 高亮），避免搜索定位与播放跟随互相抢占滚动
4. **P7 深链预留**：`detail.html` 读取 URL `?q=` 自动填入并触发搜索，P7 结果页可直接深链定位

## 关键决策记录

1. **LIKE 而非 FTS5**：单用户、单文件数百段量级，LIKE 毫秒级且中文子串匹配天然友好；FTS5 需纠结中文 tokenizer，得不偿失，留作升级路径
2. **高亮前端化**：后端只返回命中段落、不返回 `match_offsets`，消除跨语言索引差异风险

## 验证结果

后端 API 全绿（`venv/bin/python app.py` + curl）：

| 用例 | 结果 |
|------|------|
| 中文 `q=用户`（file 1） | ✅ 命中 2 条（段 1、2） |
| 通配符 `q=25%`（file 1） | ✅ 仅匹配字面"25%"1 条，**`%` 转义生效** |
| 空查询 `q=` | ✅ `{total:0, hits:[]}` |
| 无命中 `q=zzzzz` | ✅ `{total:0}` |
| 大文件 `q=平凡`（file 2，234 段） | ✅ 命中 15 条，按 segment_index 升序 |

`py_compile` 通过；IDE 对 `detail.html` 无诊断。

## 待浏览器确认（前端交互）

API 已验证，以下交互需在浏览器人工确认（CLAUDE.md：UI 改动优先 DevTools 调试）：

- [ ] 关键词高亮渲染（`<mark>` 黄底）
- [ ] `↑/↓` / 回车循环切换、当前项滚动入视图、计数正确
- [ ] 点击命中段 → 跳转播放能听到原话
- [ ] 播放 `.active`（蓝）与搜索 `.search-current`（黄）共存不闪烁
- [ ] `Esc` / `✕` 清除高亮、恢复播放跟随滚动

### P7 — 全局跨文件全文搜索 ✅ 已实现

- [x] `app.py`：新增 `GET /search`（join 查询避免 N+1，Python 按文件分组）+ Jinja `highlight` 过滤器（服务端 `<mark>` 高亮）
- [x] `templates/search.html`：结果页（统计 + 按文件分组 + 时间戳/高亮片段 + 深链跳转）+ 空状态
- [x] `templates/base.html`：导航栏全局搜索框（所有页面可见，关键词回填）
- [x] `static/style.css`：导航搜索框 + 搜索结果页样式
- [x] `templates/detail.html`：`doSearch` 增加 `onDone` 回调；URL `?q=&seg=` 深链自动定位命中段

### P7 验证结果（curl）

| 用例 | 结果 |
|------|------|
| `q=平凡`（单文件） | ✅ 1 文件组 / 15 命中 / 17 个 `<mark>` |
| `q=我们`（跨文件） | ✅ **2 文件组** / 34 命中（分组聚合正确） |
| `q=`（空查询） | ✅ 显示「全文搜索」引导，非空状态 |
| `q=zzzzz`（无命中） | ✅ search-empty 空状态 |
| 导航栏搜索框 | ✅ 首页可见、结果页关键词回填 |
| 深链 URL | ✅ `/file/2?q=平凡&seg=23`（q 编码 + seg 正确） |

`py_compile` 通过；`search.html` / `detail.html` IDE 无诊断。

### 待浏览器确认（P7 前端）

- [ ] 导航栏输入 → 回车 → 结果页（按文件分组、高亮片段）
- [ ] 点击结果项 → 跳转详情页，自动填入关键词并定位到 `seg` 命中段
- [ ] 导航栏搜索框在结果页回填关键词

## 下一步

- 用户浏览器确认 P7 交互后，提交代码（`feat: P7 全局全文搜索`）
- 全文搜索 P6 + P7 全部完成；可进入其他路线图项（说话人分离 / AI 总结 / 导出）

---

### 搜索增强：项目名检索 + 三类 Tab

> 需求：原 `/search` 只搜转写文字，文件名不参与；增加项目名检索，结果分「全部 / 项目名 / 转文字」三类。

- [x] `app.py` `/search`：新增文件名查询（`File.filename` LIKE，覆盖**所有状态**文件，含未转写完成）；读取 `tab` 参数（`all`/`name`/`content`，非法归 `all`）；计算 `name_count`/`content_count`/`total_count`（去重并集）
- [x] `templates/search.html`：Tab 栏（全部/项目名/转文字，带计数 + 当前高亮）；项目名命中块（文件名高亮 + 状态标签 + 时长）；内容命中块（沿用）；分类空态 + 全空态
- [x] `static/style.css`：Tab 栏 + 项目名命中条目样式

**决策**：文件名搜所有上传文件（转写中也能按名找到）；「全部」分两块展示（项目名命中 + 内容命中，各管各的）。

### 搜索增强验证结果（curl）

| 用例 | 结果 |
|------|------|
| `q=test_meeting`（仅项目名命中） | ✅ 项目名 1 条 / 内容 0 / Tab 计数 1·1·0 |
| `q=我们`（仅内容命中，跨文件） | ✅ 项目名 0 / 内容 2 组 |
| `tab=name`（仅项目名块） | ✅ active 在 name、无内容块 |
| `tab=content`（项目名有内容无） | ✅ 分类空态 |
| `q=zzzzz`（全空） | ✅ 全空态 |

`py_compile` 通过；`search.html` IDE 无诊断。另：清理了多轮验证遗留的 Flask debug 进程（reloader 子进程残留曾占端口）。

### 待浏览器确认（搜索增强）

- [ ] Tab 切换（全部/项目名/转文字）计数与高亮正确
- [ ] 项目名命中条目：文件名关键词高亮、状态标签、时长
- [ ] 「全部」分两块（项目名命中 + 内容命中）

## 下一步（更新）

- 用户浏览器确认后，提交代码（`feat: 搜索增强 — 项目名检索 + 全部/项目名/转文字 Tab`）
- 全文搜索（P6 单文件 + P7 全局 + 项目名维度）全部完成
